# Revisión del Arquitecto — #2 · 2026-07-24

**Privacy Compliance Skills (UE · USA · LATAM)** · Revisor: Fable (Arquitecto) · Cadencia: diaria L-V 8:00
*Guía informativa; no constituye asesoría jurídica.*

## Resumen (sin bloqueantes 🟢)

Avance real fuerte desde la revisión #1 (2026-07-09): **Fases 1 y 2 del roadmap GSD completadas** y mergeadas a `develop` vía PRs #15 y #16. La acción #1 de la revisión anterior (commit + push de las Olas 1-2 / H0) está **cumplida**. Sin desviaciones de fórmula ni de contenido legal. El fix T1 (`strict_regimes`) sigue **pendiente y correctamente diferido** a la Fase 3 — no está mal implementado, está sin implementar (marcado con comentario en `rules.ts`). Ningún hito legal cae dentro de los próximos 45 días.

## Estado git

- Rama activa: `develop` — **22 commits por delante de `origin/main`** (esperado: `main` recibe el merge al cierre del release, per STATE.md). Árbol de trabajo **limpio**, nada por commitear.
- Último commit: `d206d4a` (2026-07-23 13:32, Jose Guillermo Vasquez) — *Merge PR #16 fase-2-layout-tri-bloque*.
- Últimos hitos en log: candado de schemas en CI (QA-01), enum `verified_editorial`, loaders eu/us/latam (ESTR-01..03), `npm run validate` sobre 14 JSON.
- Nota conocida: `.git/index.lock` no se puede unlink desde el mount de Cowork (limitación esperada) — los commits/push se hacen en terminal del usuario o Claude Code.
- ⚠️ **Verificar respaldo remoto:** el tracking de `develop` apunta a `origin/main`; conviene confirmar que `develop` esté pusheada a `origin/develop` para no dejar 22 commits solo en local antes de arrancar Fase 3.

## Avance vs. plan

> **Desviación de referencia (no del proyecto):** el prompt de esta tarea compara contra la secuencia `0→H0→A→B→C→D→E→F→G` de `PROMPTS-CLAUDE-CODE-2026-07.md`. Ese documento quedó marcado **histórico el 2026-07-23**; la ejecución vigente es el **roadmap GSD de 8 Fases** en `.planning/ROADMAP.md`. Abajo, ambas vistas.

### Vista sesiones A–G (modelo histórico del prompt)

| Sesión | Estado | Evidencia |
|---|---|---|
| 0 / H0 (commits Olas 1-2) | ✅ | Mergeado (PR #15/#16); working tree limpio. Secciones marcadas OBSOLETAS en el doc |
| A (reorg `eu/us/latam`) | ✅ | `cli/rules/eu/gdpr.json`, `latam/*`, `us/*` presentes; renames ESTR-01..03 en log |
| B (+H1 fix `strict_regimes`) | ⬜ | scorer **no** referencia `region-factors`; `strict_regimes` solo como comentario "se cablea en Fase 3" en `rules.ts` |
| 1.3 (DevOps al engine) | ⬜ | `devops-penalizers.json` existe; sin integración en el engine |
| 2.2 (`doctor`) | ⬜ | `cli/src/commands/` solo tiene `audit.ts` — `doctor.ts` no existe (previsto en Fase 5) |
| C (wire USA en skills + dims) | ⬜ | Contenido `us/*` listo; wiring de skills es Fase 6 |
| D (web collections + deploy) | ⬜ | Scaffold web existe; content collections pendientes (Fase 7) |
| E (validación + golden + fixtures) | 🔄 | Validación de schemas en CI ✅ (QA-01); golden tests/fixtures ⬜ (0 fixtures, solo 2 tests unit) |
| F (naming + release 0.4.0) | 🔄 | Renombre ✅ (`name: privacy-compliance-skills`, `0.4.0-dev`); release 0.4.0 ⬜ (Fase 8) |
| G (renombre global código/assets) | ✅ | grep de marca vieja limpio en `cli/src`, `package.json`, `scripts`, `assets` |

### Vista GSD (plan vigente)

| Fase | Estado |
|---|---|
| 1 — Higiene, decisiones y renombre | ✅ COMPLETADA |
| 2 — Layout tri-bloque + candado schemas | ✅ COMPLETADA |
| 3 — Motor: F_rigor, **fix T1 strict_regimes**, USA, DevOps | ⬜ Lista para planear (`/gsd-plan-phase 3`) |
| 4 — Contrato JSON v1.1 | ⬜ |
| 5 — Doctor + fixtures + golden tests | ⬜ |
| 6 — Skills `/audit` S0–S7 tri-bloque | ⬜ |
| 7 — Web espejo del repo | ⬜ (paralelizable) |
| 8 — Release v0.4.0 | ⬜ |

**Progreso:** 12/50 tickets (24%). Punto de entrada del día: **Fase 3**.

## Salud del contenido

- Los **16 JSON** de `cli/rules/` parsean ✅ (eu/us/latam/risk-engine/schema).
- Archivos protegidos intactos: `docs/SOURCES-VALIDATION.md` (225 líneas, refs Olas 1-2-3), `docs/NORMAS-CITADAS.md` (64 líneas), `DISCLAIMER.md` presentes ✅.
- Fórmula **no** tocada fuera de `cli/src/engine`: el scorer sigue leyendo `score-formula.json`; sin duplicación de la fórmula en skills ✅. (`score-formula-v2.json` y `region-factors.json` existen pero **aún no** cableados — es exactamente el trabajo de la Fase 3.)
- Sin promoción indebida de `review_status`; el enum `verified_editorial` se añadió al schema como cambio técnico documentado (QA-01), no editorial ✅.

## Desviaciones detectadas

1. 🟡 **Referencia de la tarea desactualizada.** El prompt de la revisión diaria apunta a `PROMPTS-CLAUDE-CODE-2026-07.md` (histórico). Recomendado: actualizar el texto de la tarea programada para comparar contra `.planning/ROADMAP.md` (Fases 1-8) y `.planning/STATE.md`.
2. 🟡 **`develop` 22 commits ⏫ `origin/main`** y tracking hacia `main` — confirmar push a `origin/develop` como respaldo (ver Estado git).

Ninguna desviación 🔴 bloqueante: fórmula intacta, contenido legal intacto, T1 diferido correctamente (no mal implementado).

## 3 acciones recomendadas para HOY

1. **[Usuario → Claude Code]** Planear la **Fase 3 con Opus** (`/gsd-plan-phase 3`) y ejecutar con Sonnet: `region-factors` en el scorer (resolución peor-caso) + **fix T1 `strict_regimes` por pertenencia** (bloqueante del motor; MOTOR-01 no se mergea sin MOTOR-04/QA-04) + escalado multi-estatal USA + pilar DevOps. Es la ruta crítica.
2. **[Usuario → Terminal]** `git push origin develop` (o verificar que ya esté) para respaldar los 22 commits antes de tocar el motor. Bajo esfuerzo, protege el trabajo de Fases 1-2.
3. **[Cowork / Arquitecto]** Resolver la **decisión editorial de F_rigor de Ecuador** (incluir o no en `strict_regimes`): es dependencia de un criterio de éxito de la Fase 3 ("audit de Ecuador muestra penalizadores estrictos o assumption visible"). Secundario: Ola 3 (`argentina/peru/ecuador/panama.json`).

## Riesgos

- ⏳ **Chile Ley 21.719** — vigencia **01-12-2026** (~130 días, fuera de la ventana de 45). Instrucciones APDP saliendo jun–nov 2026; vigilancia mensual programada (`docs/VIGILANCIA-NORMATIVA.md`). Sin urgencia hoy.
- 🟡 **Descrybe MCP** sin autenticar → verificación fina estado-por-estado USA (19 leyes) sigue pendiente; requerido antes de promover `us/state-matrix.json` a validado.
- 📋 **Backlog no-código (Cowork):** Fase AP (no existe `knowledge/anti-patterns/` ni skill `/fix`), iteración 2 del eval de `/audit` (distinguir `unknown` de `absent`), Ola 3 de países.
- ⏳ Golden tests/fixtures aún inexistentes: la red de seguridad del motor llega en Fase 5 — mantener disciplina de no congelar interfaces antes de Fase 4.

---
*Próxima revisión automática: siguiente día hábil 8:00 (tarea `revision-diaria-arquitecto-pcs`).*
