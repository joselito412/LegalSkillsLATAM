# Revisión del Arquitecto — #8 · 2026-08-03 (lunes)

**Privacy Compliance Skills (UE · USA · LATAM)** · Revisor: Fable (Arquitecto) · Cadencia: diaria L-V 8:00
*Guía informativa; no constituye asesoría jurídica.*

## Resumen (sin 🔴 bloqueantes · una 🟠 alta nueva)

**Se rompió la inercia.** El domingo 2026-08-02 hubo un sprint de **5 commits** que ejecutó las tres acciones recomendadas la semana pasada y además **arrancó la Fase 4**: los reportes de revisión #5–#7 quedaron commiteados, `STATE.md` se puso al día, la ronda de vigilancia 2026-08 entró al repo, y aterrizó `architecture/AGENT-CONTRACT-V1.1-DESIGN.md` — la decisión de semántica que el propio ROADMAP exigía tomar en sesión Fable **antes** de implementar. La Fase 4 pasa de ⬜ a 🔄 con el paso 1 de 8 ya en código.

Todo sigue verde: `tsc --noEmit` exit 0, **80/80 tests** (+3 respecto de 77), 16/16 JSON, cero degradación de contenido protegido, fórmula viviendo solo en `cli/src/engine/`, fix T1 intacto.

**Lo importante de hoy no es el avance sino lo que el diseño destapó.** Dos hallazgos cambian supuestos que el repo daba por buenos:

1. **🟠 El manifiesto del plugin contradice a la skill.** `.claude-plugin/plugin.json` declara para `audit`: `tools: []`, `file_access: "none"`, `network: false`, `system_commands: false`. `skills/audit/SKILL.md:29` instruye ejecutar `npx privacy-compliance-skills audit --config --json`, y su §276 declara que lee y escribe `legalskills.config.json` y `.legalskills/last-audit.json`. **Si el runtime del marketplace aplica el manifiesto literalmente, el canal de la CLI es inalcanzable para el agente empaquetado** — y con él, la propuesta de valor central del proyecto ("la CLI es la única implementación autorizada del score"). El agente caería siempre al modo fallback, que por diseño se etiqueta *"estimado, no verificado"*. Es anterior al contrato y **ninguna forma del JSON lo arregla**.
2. **Una premisa documentada durante semanas era falsa, y la medición del 02-08 la corrigió.** `.planning/REQUIREMENTS.md` afirmaba que "el pool plano nunca subreporta" frente a la fórmula del ADR-001. El barrido de **217.728 combinaciones** mostró un 0,32 % de casos donde el ADR da **+1** sobre el motor vivo. La magnitud es trivial y ningún caso cambia de nivel — pero la afirmación, tal como estaba escrita, no se sostenía. **Autocorrección sana y bien documentada.**

## Estado git

- Rama activa: `develop` @ `ee0d2c7` (2026-08-02 11:51, *feat(contrato-01): passthrough de topic*). **5 commits nuevos** desde la revisión #7 — todos del domingo 02-08 entre las 11:09 y las 11:51.
- **Working tree limpio ✅.** `git status -sb` → una sola línea (`## develop...origin/main [ahead 49]`). **Cero archivos untracked, cero cambios sin commitear.** Es la primera vez en cuatro revisiones que no hay trabajo huérfano en el working tree.
- **Delta auditado** (`git diff c4f734d..HEAD --stat`): 14 archivos, +583/−11. Reparto: 3 reportes de revisión, `architecture/AGENT-CONTRACT-V1.1-DESIGN.md` (nuevo, 139 líneas), +49 en `docs/SOURCES-VALIDATION.md` (ronda de vigilancia), 4 archivos del motor con el passthrough de `topic`, 3 archivos de test (+55), y `REQUIREMENTS.md`/`STATE.md` actualizados.
- ⚠️ **`origin/main` @ `9f8fbee` (2026-07-09)** — `rev-list --left-right --count origin/main...develop` → **0 / 49**. **25 días de atraso público**, 49 commits. El merge sigue siendo limpio (0 commits divergentes).
- 🟡 La rama **local** `main` sigue `ahead 4 / behind 2` — sin cambio, cuarta revisión. `STATE.md:63` ya documenta que **su reconciliación no es prerrequisito** del PR (sus 4 commits son las versiones pre-squash de los PRs #13/#14 ya presentes en `origin/main`; `git diff main develop --diff-filter=D` vacío). Se retira de la lista de fricciones.
- ✅ **Frescura recuperada:** `.git/FETCH_HEAD` es del **2026-08-02 11:08** (ayer, no de hace 3 días). La lectura de `origin/*` es confiable.
- Nota conocida: el `.git` montado en Cowork no permite `unlink` del `index.lock` — commits y push van por la terminal del usuario o Claude Code. Lecturas sin problema.

## Avance vs. plan

> **Sobre la secuencia A–G del prompt.** La vista `0→H0→A→B→C→D→E→F→G` de `PROMPTS-CLAUDE-CODE-2026-07.md` está *superseded* desde el 23-07 y la revisión #7 la retiró. Se conserva solo la verificación puntual de evidencia que pide el prompt, plegada a la columna de evidencia: `cli/rules/eu/gdpr.json` existe (A ✅) · `rules.ts:320` resuelve `strict_regimes.members` y `scorer.ts:179` lo consume (B + fix T1 ✅) · `cli/src/commands/` sigue con **solo** `audit.ts`, sin `doctor.ts` (2.2 ⬜, reubicado a Fase 5) · 10 archivos de test con **80 casos** (E 🔄).
> **Acción pendiente por octava vez:** reescribir el paso 2 de la tarea programada contra las Fases 1–8 de `.planning/ROADMAP.md`.

### Vista GSD (plan vigente — `.planning/ROADMAP.md`)

| Fase | Estado | Evidencia verificada hoy |
|---|---|---|
| 1 — Higiene, decisiones y renombre | ✅ | — |
| 2 — Layout tri-bloque + candado schemas | ✅ | 16 JSON: `eu/`(1) `latam/`(5) `us/`(2) `risk-engine/`(6) `schema/`(2) |
| 3 — Motor: F_rigor, fix T1, USA, DevOps | ✅ | Cerrada 2026-07-28 · `state-matrix.json` mantiene **19 estados** (test lo asevera), conforme a la corrección de la ronda 08 |
| **4 — Contrato JSON v1.1** | 🔄 **arrancada (paso 1 de 8)** | `AGENT-CONTRACT-V1.1-DESIGN.md` con D1–D9 + orden de ejecución · `topic?: string` en `rules.ts:80` y `scorer.ts:56-60` · +3 tests. **Falta:** `cli/src/contract/` no existe · `grep schema_version cli/src/**` → **0** · el JSON Schema del contrato sigue sin commitear (`cli/rules/schema/` solo tiene country-rules y us-state-matrix) |
| 5 — Doctor + fixtures + golden | ⬜ | `cli/src/commands/` = solo `audit.ts` — bloqueada por Fase 4 |
| 6 — Skills `/audit` S0–S7 | ⬜ | Bloqueada por Fase 4 **y por la 🟠 de permisos del plugin (D-01 abajo)** |
| 7 — Web espejo del repo | ⬜ | **Paralelizable desde Fase 2 — 5ª revisión consecutiva sin arrancar y sin bloquear a nadie** |
| 8 — Release v0.4.0 | ⬜ | `package.json` en `0.4.0-dev` |

**Progreso: 20/54 tickets (37 %).** Sin ticket cerrado formalmente — CONTRATO-01 sigue `Pending` en la tabla de `REQUIREMENTS.md:241` pese a tener el paso 1 en código (ver D-03).

### Fase 4 — orden de ejecución fijado por el diseño

| # | Trabajo | Ticket | Estado |
|---|---|---|---|
| 1 | Passthrough de `topic` (DevOps) | CONTRATO-01 | ✅ hecho 02-08 |
| 2 | Colapsar la duplicación de los 9 penalizadores clásicos (D8) | CONTRATO-01 | ⬜ **siguiente** |
| 3 | Cablear `scorer.ts` a `score-formula.json.penalizers` para heredar `legal_refs` (D4) | CONTRATO-01 | ⬜ (depende de 2) |
| 4 | Serializador + `schema_version` + `findings[]` + JSON Schema nuevo | CONTRATO-01 | ⬜ |
| 5–7 | `jurisdictions[]`/`recipe_ref` · `unknown` vs `absent` · `review_status` por finding | CONTRATO-02/04/05 | ⬜ |
| 8 | Consolidar `AGENT-CONTRACT.md` + resolver la divergencia numérica | CONTRATO-03 | ⬜ |

**Dato de dimensionamiento que el diseño destapó:** el motor puede emitir **21 penalizadores, no 13**; solo 7 (DevOps) se alimentan de JSON con metadata rica, y `fe-penalizers.json`, `be-penalizers.json` y los bloques `penalizers` de cada país **no tienen un solo call-site** en `cli/src`. La doble fuente de verdad (B5) es mayor de lo que documentaba el repo.

## Salud del contenido

- **16/16 JSON de `cli/rules/` parsean ✅** (`json.load` recursivo).
- **Build y tests verdes ✅** (copia limpia en sandbox, Node 22.22.3): `npx tsc --noEmit` **exit 0** · `node --test tests/*.test.js` → **80 pass / 0 fail** (642 ms), 10 archivos.
- **La fórmula sigue viviendo solo en `cli/src/engine/` ✅.** Grep de `F_rigor|scoreFinal|rigorFactor|min(100` fuera de `engine/` → **una única coincidencia, y es un comentario** (`audit.ts:71`).
- **Fix T1 intacto ✅.** `resolveRigorFactor()` resuelve por **pertenencia** a `strict_regimes.members` (`rules.ts:304-337`), consumido por `scorer.ts`. Cero comparaciones numéricas de F_rigor. `region-factors.json` v1.1.0 conserva el bloque `strict_regimes` con su `description` citando el hallazgo T1, y `EC` con su `basis` declarando que el default del motor **no** es determinación editorial.
- **Archivos protegidos — sin degradación ✅.** `docs/SOURCES-VALIDATION.md` pasó de 259 a **308 líneas**: el crecimiento es **aditivo** (ronda de vigilancia 2026-08 anexada), con **Ola 1 en L34 y Ola 2 en L176 intactas**. `docs/NORMAS-CITADAS.md` sigue en 64 líneas. `git diff HEAD` sobre ambos → vacío.
- **`review_status` — sin promociones indebidas ✅.** `brasil`, `colombia`, `mexico` = `verified_editorial` (promovidos por el owner humano el 28-07 conforme a `docs/GOVERNANCE.md:79`); los otros 11 = `pending_legal_validation`. **`reviewed_by: null` en los 14 → nada está `validated`.** Sin cambios desde la revisión #7.
- **Disclaimers intactos ✅:** **12/12 `SKILL.md`** con cláusula de no-asesoría · `DISCLAIMER.md` · `README.md:21` · `README.en.md:21` (*"does not constitute or replace professional legal advice"*).

## Desviaciones detectadas

Ninguna 🔴. **Una 🟠 alta (arquitectónica, preexistente, recién visible) y tres 🟡.**

| # | Severidad | Desviación | Evidencia | Efecto |
|---|---|---|---|---|
| **D-01** | 🟠 **ALTA** | **El manifiesto del plugin niega el canal que la skill necesita.** `.claude-plugin/plugin.json` → `skill_permissions.audit`: `tools: []`, `file_access: "none"`, `network: false`, `system_commands: false`, descripción *"No external calls"* | `skills/audit/SKILL.md:29` ordena `npx privacy-compliance-skills audit --config --json`; `SKILL.md:276` declara lectura/escritura de `legalskills.config.json` y `.legalskills/last-audit.json` | Si el runtime aplica el manifiesto, el agente empaquetado **nunca** alcanza la CLI y opera siempre en fallback etiquetado *"estimado, no verificado"*. Invalida el supuesto central de la Fase 6. **Las mismas 4 llaves están en las 6 skills del manifiesto**, no solo en `audit` |
| **D-02** | 🟡 | **Recall fallido de la vigilancia**, autorreportado por la propia ronda | La *Circular Externa 003 SIC* (19-12-2025, Diario Oficial 02-01-2026) **no se detectó en las dos pasadas de julio** pese a estar publicada desde enero | La metodología de `docs/VIGILANCIA-NORMATIVA.md` tiene un hueco de cobertura en normativa **secundaria** de la SIC. Merece ticket propio, no solo la nota §6 |
| **D-03** | 🟡 | **Desfase de tracking:** el paso 1 de CONTRATO-01 está en código, pero `REQUIREMENTS.md:241` y el contador de `STATE.md` siguen en `Pending` / 20 tickets | `git log` `ee0d2c7` vs. tabla de tickets | Menor, pero es exactamente el patrón que hizo que `STATE.md` quedara 5 días desfasado en la Fase 3. Cerrarlo al cierre de cada paso, no al cierre de la fase |
| **D-04** | 🟡 | **`origin/main` congelado 25 días / 49 commits**, con la tensión **registrada pero no resuelta** | `STATE.md:63` documenta ambas posiciones ("merge al cierre del release" del owner vs. "merge ya" del Arquitecto ×3) y las declara *no reconciliadas* | El costo ya no es el merge: es el **bucle abierto**. Recomendarlo por quinta vez sin argumento nuevo es el meta-riesgo que la revisión #7 señaló. Hoy se convierte en petición de **decisión binaria**, no de ejecución |

## 3 acciones recomendadas para HOY (lunes)

| # | Acción | Responsable | Tiempo | Por qué hoy |
|---|---|---|---|---|
| **1** | **Fase 4, pasos 2 y 3.** Sonnet: colapsar la duplicación de los 9 penalizadores clásicos a una sola fuente (D8) y cablear `scorer.ts` a `score-formula.json.penalizers` para heredar los `legal_refs` de los 6 que ya las tienen (D4). Opus valida contra los criterios de CONTRATO-01. Actualizar el estado del ticket en el mismo commit (cierra D-03) | **Claude Code** | ~2 h | Es el camino crítico: el paso 4 (serializador + `schema_version` + JSON Schema) depende de ambos, y las Fases 5, 6 y 8 dependen del 4. El sprint del domingo dejó el diseño listo; hoy es el día de menor fricción para continuar |
| **2** | **Redactar `ADR-003 — permisos del plugin vs. canal CLI`** con las tres salidas y su costo: (a) ampliar el manifiesto a `system_commands: true` acotado al binario propio, (b) aceptar el fallback como modo por defecto del plugin y reetiquetar la propuesta de valor, (c) canal alterno. **Redactar, no aplicar** — la decisión es del owner | **Cowork (Arquitecto)** | ~45 min | D-01 invalida el supuesto de la Fase 6 y **no depende de la Fase 4**. Resolverlo ahora evita construir un contrato para un canal inalcanzable. Es el hallazgo con mayor relación impacto/costo del día |
| **3** | **Cerrar el bucle de `main`: decidir, no necesariamente mergear.** Escribir **una línea** en `STATE.md` que fije la posición del owner — «`develop`→`main` se mergea hoy» **o** «`main` espera al tag v0.4.0, y esta revisión deja de proponerlo». Cualquiera de las dos cierra D-04 | **Usuario** | ~5 min | Es lo único que corta un bucle de cuatro revisiones. Ambas posiciones son defendibles; la que no es defendible es dejarla abierta una semana más. Si la decisión es "mergear", ejecutarlo en terminal (merge limpio, 0 divergentes) |

**Alternativa si el día se cae:** ejecutar la #3 (5 minutos) y la #2. La #1 tiene el mayor valor pero es la única que puede reprogramarse sin costo acumulativo.

## Riesgos y fechas

**Hitos ≤45 días — uno, y es nuevo:**

- 🔴 **Brasil · ECA Digital (Lei 15.211/2025), Etapa II del cronograma oficial: agosto-2026 — ventana abierta AHORA.** La ANPD publica en este mes las *orientaciones definitivas de aferición de edad*. La ronda 2026-08 lo marcó ✅ CONFIRMADO con fuente oficial y lo calificó de **régimen adyacente no cubierto por el repo**; la propuesta editorial es un bloque `minors_digital` en `brasil.json` (aferición de edad, supervisión parental, publicidad a menores). Alta relevancia para devs. Reglamentos de fiscalización/sanciones desde nov-2026; fiscalización desde ene-2027.
- 🔴 **Brasil · Lei 15.352/2026 (25-02-2026): la ANPD pasa a agencia reguladora.** Ticket editorial prioritario: `brasil.json` → `authority` (naturaleza jurídica y competencias) y revisar toda mención a la ANPD como "autarquía vinculada". `brasil.json` está en `verified_editorial` — **la promoción quedó desactualizada por un cambio normativo posterior**, lo que conviene reflejar antes de que el archivo se presente como autoritativo.

**Fuera de ventana (contexto):** Chile Ley 21.719 → **01-12-2026, faltan 120 días**; la ventana de instrucciones APDP (jun–nov 2026) sigue **abierta y sin publicaciones**, agravada por el Consejo Directivo aún no constituido — `chile.json` correctamente en `pending_legal_validation`. Colombia Ley 2573/2026 → régimen general ~19-11-2026 (108 días). Consulta pública EDPB sobre las Guidelines 02/2026 (anonimización) y 03/2026 (scraping en IA generativa) → cierra **30-10-2026** (88 días): son **borradores**, prohibido citarlas como criterio firme.

**Pendientes conocidos:**

- **Ola 3 (`argentina`, `peru`, `ecuador`, `panama`.json): sin arrancar** — quinta revisión consecutiva. `cli/rules/latam/` conserva 4 países + `_template.json`. El insumo de Ecuador es hoy **desbordante** (la ronda 08 catalogó 4 resoluciones 2026 nuevas y 7 sustantivas de 2025 aún no incorporadas: 0041-R interés legítimo, 0030-R anonimización, 0028-R Reglamento del DPD, entre otras) y el de Perú está cerrado (el ODP es obligación **ya exigible** desde el 30-06-2026). Sigue siendo el trabajo editorial con mejor relación insumo/esfuerzo del backlog.
- **Panamá ANTAI-DG-003-2026:** 🟡 PROBABLE por **tercera ronda consecutiva**; verificado el 02-08 que **no figura** en la sección oficial de Resoluciones ni lo devuelve el Legal Data Hunter. Escalado al editor para solicitud formal a ANTAI o búsqueda por número de Gaceta. Bloquea el `international_transfer` del futuro `panama.json`.
- **Backlog editorial derivado de la ronda 08:** `colombia.json` → CCM de la Circular 003 (facultativo pero vinculante al adoptarse) · `usa-federal.json` → capa de reglamentos CPPA (ADMT exigible 01-01-2027, evaluaciones de riesgo desde 01-01-2026) · `gdpr.json` → Guidelines blockchain 02/2025, ya finales.
- **Verificación estado-por-estado USA:** el pendiente heredado **quedó cerrado con corrección** — el IAPP Tracker confirma **19 leyes integrales, no 20**, y el motor mantiene 19 (aseverado por test). Un pendiente menos.
- **AUD-03 (ratificar Ecuador en `strict_regimes`):** desbloqueado desde el 28-07, **6 días sin decidirse**. `EC` sigue en `pending_editorial_decision` con `engine_default: strict`.
- **Fase AP (anti-patrones + `/fix`) e iteración 2 del eval de `/audit`:** sin arranque; ambas dependen del contrato v1.1.
- **Meta-riesgo (revisado a la baja):** el patrón "días parados + sprint" se repitió, pero esta vez **el sprint ejecutó las tres acciones recomendadas**. La revisión diaria sí tuvo función correctiva. Se retira la propuesta de bajar la cadencia a L/X/V; se mantiene en observación una semana más.

---
*Privacy Compliance Skills — [DISCLAIMER.md](../../DISCLAIMER.md)*
*Guía informativa. No constituye asesoría jurídica.*
