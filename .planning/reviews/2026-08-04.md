# Revisión del Arquitecto — #9 · 2026-08-04 (martes)

**Privacy Compliance Skills (UE · USA · LATAM)** · Revisor: Fable (Arquitecto) · Cadencia: diaria L-V 8:00
*Guía informativa; no constituye asesoría jurídica.*

## Resumen (sin 🔴 bloqueantes · dos 🟠 altas sin dueño)

**Ayer no se ejecutó ninguna de las tres acciones recomendadas — y aun así fue un buen día.** Los 3 commits del 03-08 no movieron un solo ticket, pero produjeron el hallazgo de mayor alcance del milestone: una **re-verificación ciega** midió que los JSON de reglas declaran **68 penalizadores y el motor consume 8**. Los 60 restantes nunca influyen en ningún score. Eso abrió `MOTOR-10` y, de paso, corrigió dos afirmaciones del diseño del contrato con un segundo barrido independiente (86.016 combinaciones contrastadas contra las 217.728 del 02-08).

Es la segunda vez en dos días que este proyecto **se autocorrige contra su propia documentación**. Vale más que el ticket que no se cerró.

**Pero hay que nombrar el costo:** dos días hábiles sin una línea de código de producto, y las tres acciones de ayer siguen abiertas — incluida la #3, que cuesta cinco minutos y va por su quinto día.

**El punto arquitectónico de hoy es que los dos hallazgos abiertos golpean lo mismo.** D-01 (permisos del plugin) dice *el agente empaquetado no puede alcanzar la CLI*; MOTOR-10 dice *y si la alcanzara, el score apenas distingue jurisdicciones*. Juntos cuestionan la propuesta de valor completa —"cobertura UE · USA · LATAM con la CLI como única implementación del score"— y **ninguno de los dos depende de la Fase 4**. Van juntos a una sola sesión de decisión, hoy.

Salud técnica intacta: `tsc --noEmit` exit 0 · **80/80 tests** · `validate.js` 14/14 con 0 errores de schema · 16/16 JSON parsean · fórmula viviendo solo en `cli/src/engine/` · fix T1 intacto · cero degradación de contenido protegido.

## Estado git

- Rama activa: `develop` @ **`cd6317f`** (2026-08-03 10:16). **3 commits nuevos** desde la revisión #8, todos del lunes 03-08 entre las 10:10 y las 10:16.
  - `6e29f37` — corrige dos afirmaciones del diseño del contrato tras verificación independiente
  - `badda7d` — reporte de revisión #8
  - `cd6317f` — abre `MOTOR-10` (60 de 68 penalizadores sin cablear)
- **Working tree limpio ✅.** `git status --porcelain` vacío. Segunda revisión consecutiva sin trabajo huérfano. *(El `warning: unable to unlink .git/index.lock` es la limitación conocida del mount de Cowork, no un cambio pendiente.)*
- ✅ **`develop` está pusheada al día.** El reflog de `origin/develop` registra **tres `update by push`** el 03-08 (10:10, 10:14, 10:16), uno por commit. `develop...origin/develop` → **0 / 0**. La lectura de `origin/develop` es confiable pese a que `FETCH_HEAD` sea del 02-08: el ref remoto se actualizó por push, no por fetch.
- ⚠️ **`origin/main` @ `9f8fbee` (2026-07-09).** `rev-list --left-right --count origin/main...develop` → **0 / 52**. **26 días de atraso público, 52 commits.** El merge sigue siendo limpio (0 commits divergentes). Quinta revisión consecutiva señalándolo.
- **Delta auditado** (`git diff ee0d2c7..HEAD --stat`): **3 archivos, +147 / −17**. Reparto: `.planning/reviews/2026-08-03.md` (nuevo, 112 líneas), `REQUIREMENTS.md` (+26/−? — abre MOTOR-10 y reescribe CONTRATO-03), `AGENT-CONTRACT-V1.1-DESIGN.md` (correcciones de método). **Cero archivos de `cli/src/`.**
- Nota conocida: el `.git` montado en Cowork no permite `unlink` del `index.lock` — commits y push van por la terminal del usuario o Claude Code. Lecturas sin problema.

## Avance vs. plan

> **Sobre la secuencia A–G del prompt.** `PROMPTS-CLAUDE-CODE-2026-07.md` está *superseded* desde el 23-07; la revisión #7 la retiró de la vista principal. Se conserva solo la verificación puntual de evidencia que pide el paso 2 del prompt, plegada a la columna de evidencia: `cli/rules/eu/gdpr.json` existe (A ✅) · `rules.ts:320` resuelve `strict_regimes.members` y `scorer.ts:100` documenta la pertenencia (B + fix T1 ✅) · `cli/src/commands/` sigue con **solo** `audit.ts`, sin `doctor.ts` (2.2 ⬜, reubicado a Fase 5) · 10 archivos de test con **80 casos** (E 🔄).
> **Acción pendiente por novena vez:** reescribir el paso 2 de la tarea programada `revision-diaria-arquitecto-pcs` contra las Fases 1–8 de `.planning/ROADMAP.md`. Cuesta un minuto y ahorra este párrafo cada día.

### Vista GSD (plan vigente — `.planning/ROADMAP.md`)

| Fase | Estado | Evidencia verificada hoy |
|---|---|---|
| 1 — Higiene, decisiones y renombre | ✅ | — |
| 2 — Layout tri-bloque + candado schemas | ✅ | 16 JSON: `eu/`(1) `latam/`(5) `us/`(2) `risk-engine/`(6) `schema/`(2) · `validate.js` 14/14, 0 errores |
| 3 — Motor: F_rigor, fix T1, USA, DevOps | ✅ | Cerrada 2026-07-28 · fix T1 vivo en `rules.ts:320` + `scorer.ts:100` |
| **4 — Contrato JSON v1.1** | 🔄 **sin avance — sigue en el paso 1 de 8** | `topic?: string` en `rules.ts:80` y `scorer.ts:56-60` (paso 1, del 02-08). **Sin cambios:** `cli/src/contract/` no existe · `grep schema_version cli/src/**` → **0** · el JSON Schema del contrato sigue sin commitear (`cli/rules/schema/` solo tiene `country-rules` y `us-state-matrix`) |
| 5 — Doctor + fixtures + golden | ⬜ | `cli/src/commands/` = solo `audit.ts` — bloqueada por Fase 4 |
| 6 — Skills `/audit` S0–S7 | ⬜ | Bloqueada por Fase 4 **y por D-01 (permisos del plugin), sin ADR por 2º día** |
| 7 — Web espejo del repo | ⬜ | `web/src/content` **no existe** — **6ª revisión consecutiva sin arrancar**, paralelizable desde la Fase 2, sin bloquear a nadie |
| 8 — Release v0.4.0 | ⬜ | `package.json` en `0.4.0-dev` |

**Progreso: 20/55 tickets (36 %).** El denominador subió de 54 a 55 con `MOTOR-10`. Ningún ticket cerrado ayer.

### Fase 4 — orden de ejecución (sin movimiento desde el 02-08)

| # | Trabajo | Ticket | Estado |
|---|---|---|---|
| 1 | Passthrough de `topic` (DevOps) | CONTRATO-01 | ✅ hecho 02-08 |
| 2 | Colapsar la duplicación de los penalizadores clásicos (D8) | CONTRATO-01 | ⬜ **siguiente — arrastre de ayer** |
| 3 | Cablear `scorer.ts` a `score-formula.json.penalizers` para heredar `legal_refs` (D4) | CONTRATO-01 | ⬜ (depende de 2) |
| 4 | Serializador + `schema_version` + `findings[]` + JSON Schema nuevo | CONTRATO-01 | ⬜ **la forma de `findings[]` depende de MOTOR-10** |
| 5–7 | `jurisdictions[]`/`recipe_ref` · `unknown` vs `absent` · `review_status` por finding | CONTRATO-02/04/05 | ⬜ |
| 8 | Consolidar `AGENT-CONTRACT.md` + resolver la divergencia numérica | CONTRATO-03 | ⬜ |

**Re-dimensionamiento del paso 2 (aporte de ayer, `6e29f37`).** No es un *dedupe* mecánico: de los 9 penalizadores clásicos, **7 se colapsan a una definición única** y **2 conservan su descomposición por pilar** (`minors_data_fe/be` y sus pares), porque el desglose es deliberado — consentimiento parental en la UI y restricciones técnicas en el backend son remediaciones distintas con dueños distintos. La primera medición lo simplificó de más. Quien implemente debe leer esto antes de tocar `frontend-scorer.ts`/`backend-scorer.ts`.

### 🟠 MOTOR-10 — el hallazgo que re-dimensiona el milestone

Abierto ayer por re-verificación ciega. Lo confirmo y lo elevo:

- **Medido:** 68 penalizadores declarados en los JSON de reglas · **8 consumidos** por el motor (los 7 de `devops-penalizers.json` + `us_multistate_exposure`). Los **60 restantes nunca influyen en ningún score**: `gdpr.json`(8), `brasil.json`(7), `colombia.json`(8), `mexico.json`(7), `chile.json`(6), 7 más de `usa-federal.json`, `fe-penalizers.json`(5), `be-penalizers.json`(6).
- **Trampa metodológica que el propio ticket documenta:** los 6 de `score-formula.json` tampoco están cableados. Sus ids **coinciden** con literales escritos a mano en TypeScript, así que verificarlos por coincidencia de ids da un **falso positivo**. Es exactamente el patrón que produjo el bug T1.
- **Consecuencia observable:** el score es hoy **casi ciego a la jurisdicción**. Lo único que varía por país es `F_rigor`, la pertenencia a `strict_regimes` (3 penalizadores), la supresión de transferencias por adecuación y el penalizador multi-estatal de USA. Dos proyectos idénticos en Colombia y en la UE reciben **los mismos hallazgos**, con distinto multiplicador.
- **Por qué es decisión de arquitecto y no tarea de ejecución:** cablear 60 penalizadores **cambiaría los scores de forma masiva**. Es una decisión de producto con impacto numérico y bump de `schema_version`, no un fix.
- **Por qué hoy y no después del paso 4:** `findings[]` es la estructura que transporta los hallazgos al LLM. Si los 60 entran al motor, los findings son otros. Decidir después de escribir el serializador obliga a rehacerlo.

## Salud del contenido

| Chequeo | Resultado |
|---|---|
| JSON de `cli/rules/` parsean (`json.load`) | ✅ **16/16** |
| `node scripts/validate.js` | ✅ **14/14 archivos · 0 errores de schema · 7 advertencias** (preexistentes) |
| `npx tsc --noEmit` | ✅ **exit 0** |
| `npm test` | ✅ **80/80 pass, 0 fail** |
| Fórmula fuera de `cli/src/engine/` | ✅ **no.** El grep marca `commands/audit.ts`, pero es **un comentario** (`audit.ts:71`), no código. Falso positivo descartado |
| Fix T1 (pertenencia, no igualdad numérica) | ✅ `rules.ts:320` lee `strict_regimes.members`; `scorer.ts:100` lo documenta explícitamente |
| `docs/SOURCES-VALIDATION.md` | ✅ **308 líneas.** Ola 1 (§34) y Ola 2 (§176) íntegras, con las 3 rondas de vigilancia (07 en dos pasadas, 08) añadidas **sin desplazarlas** |
| `docs/NORMAS-CITADAS.md` | ✅ **64 líneas**, 5 secciones (UE · USA · LATAM · estándares · notas del revisor), 11 jurisdicciones + 10 estándares. Disclaimer en línea 64 intacto |
| Disclaimers en el repo | ✅ **60 archivos `.md`** con "no constituye asesoría jurídica" |
| `review_status` promovido sin revisor | ✅ **no.** 3 en `verified_editorial` (brasil, colombia, mexico) · 11 en `pending_legal_validation` · **cero en `validated`** — correcto, `validated` exige abogado revisor |

**⚠️ Matiz sobre `brasil.json`.** Está en `verified_editorial`, pero la ronda de vigilancia 08 confirmó la **Lei 15.352/2026** (25-02-2026), que convierte a la ANPD en agencia reguladora. La promoción quedó desactualizada por un cambio normativo **posterior** a la validación. No es una degradación (nadie tocó el archivo indebidamente), es deuda editorial: conviene reflejarlo antes de que el archivo se presente como autoritativo. Señalado por segundo día.

## Desviaciones detectadas

Ninguna 🔴 bloqueante. La fórmula no se tocó fuera de `cli/src/engine/`, no se editó contenido legal sin `review_status`, y el fix T1 está bien implementado.

| # | Desviación | Sev. | Días abierta |
|---|---|---|---|
| **D-01** | **Permisos del plugin vs. canal CLI.** `.claude-plugin/plugin.json` declara para las **5 skills** (`audit`, `clasificar-datos`, `privacy-check`, `risk-score`, `matriz-normativa`): `tools: []`, `file_access: "none"`, `network: false`, `system_commands: false`. `skills/audit/SKILL.md:29` instruye ejecutar `npx privacy-compliance-skills audit --config --json`. **Verificado hoy: sin cambios, y afecta a las 5 skills, no solo a `audit`.** Si el runtime del marketplace aplica el manifiesto literalmente, el agente empaquetado cae **siempre** al fallback "estimado, no verificado". Ningún JSON del contrato lo arregla. **ADR-003 fue la acción #2 de ayer y no se redactó** | 🟠 | 2 |
| **D-05** | **MOTOR-10 sin decisión.** 60 de 68 penalizadores sin call-site; el score es casi ciego a la jurisdicción. Abierto ayer; las tres salidas están escritas, falta elegir. Gatea el paso 4 de la Fase 4 | 🟠 | 1 |
| **D-03** | **`CONTRATO-01` sigue `Pending`** en `REQUIREMENTS.md:248` pese a tener el paso 1 de 8 en código desde el 02-08. El estado del ticket no refleja el repo | 🟡 | 2 |
| **D-04** | **Bucle de `main` sin cerrar.** `STATE.md:63` documenta "merge al cierre del release"; las revisiones #5–#8 recomiendan mergear ya. **Ambas posiciones son defendibles; lo que no lo es, es dejarla abierta.** `STATE.md` sigue sellado el 02-08 | 🟡 | 5 |
| **D-02** | **Texto de la tarea programada** compara contra la secuencia A–G *superseded*, no contra las Fases 1–8 del ROADMAP | 🟡 | 9 |
| **D-06** | **`score-formula-v2.json` huérfano.** Declara la fórmula dual por pilares (v2.0.0) y **no tiene un solo call-site** en `cli/`. Es la tercera fuente de fórmula en el árbol de reglas, junto a `score-formula.json` (viva) y `ADR-001`. Ya registrado el 24 y 27-07; alimenta la divergencia numérica de CONTRATO-03. Riesgo: alguien lo lee como autoritativo | 🟡 | 11 |

**Sobre la desviación de prioridad de ayer:** cero de tres acciones ejecutadas, pero lo que se hizo en su lugar tiene mayor valor arquitectónico y **re-dimensiona** dos de ellas. No lo cuento como desviación — lo cuento como coste de oportunidad acumulado: **dos días hábiles sin código de producto**, con la Fase 4 en el paso 1 de 8 y las Fases 5, 6 y 8 detrás.

## 3 acciones recomendadas para HOY (martes)

| # | Acción | Responsable | Tiempo | Por qué hoy |
|---|---|---|---|---|
| **1** | **Una sola sesión Fable de decisión, con dos salidas escritas: `ADR-003` (permisos del plugin) y la decisión de `MOTOR-10`.** ADR-003 con sus tres opciones y costo: (a) ampliar el manifiesto a `system_commands: true` acotado al binario propio, (b) aceptar el fallback como modo por defecto y reetiquetar la propuesta de valor, (c) canal alterno. MOTOR-10 con las tres del ticket: los penalizadores por país entran al motor · se marcan como catálogo editorial no-ejecutable (`scoring_treatment.generates_penalizer: false`) · se fusionan con los genéricos. **Redactar y decidir — el owner firma** | **Cowork (Arquitecto) + Usuario** | ~1,5 h | Van juntos porque atacan **lo mismo**: si el agente no alcanza la CLI (D-01) y el score apenas distingue jurisdicciones (D-05), la propuesta de valor del repo no se sostiene tal como está escrita. Ninguno depende de la Fase 4, y MOTOR-10 gatea su paso 4. Separarlos en dos sesiones es lo que hizo que D-01 se cayera ayer |
| **2** | **Fase 4, pasos 2 y 3** (arrastre). Sonnet: colapsar **7** penalizadores duplicados a una fuente única **conservando la descomposición por pilar de los 2 restantes** (ver `6e29f37`), y cablear `scorer.ts` a `score-formula.json.penalizers` para heredar los `legal_refs`. Opus valida contra los criterios de CONTRATO-01. **Actualizar el estado del ticket en el mismo commit** (cierra D-03) | **Claude Code** | ~2 h | **No está bloqueado por la #1**: el propio MOTOR-10 acota que CONTRATO-01 solo hereda `legal_refs` de los penalizadores que el motor **ya** emite. Es lo único que mueve tickets, y va por su segundo día de arrastre. Corre en paralelo con la #1 (distinto responsable) |
| **3** | **Una línea en `STATE.md` que cierre el bucle de `main`:** «`develop`→`main` se mergea hoy» **o** «`main` espera al tag v0.4.0, y esta revisión deja de proponerlo». Cualquiera de las dos cierra D-04 | **Usuario** | ~5 min | Quinto día. 52 commits, 26 días de atraso público, merge limpio verificado. Si la decisión es mergear, se ejecuta en terminal. **Añadir un minuto para D-02:** reescribir el paso 2 de la tarea programada contra el ROADMAP |

**Si el día se cae:** ejecutar la #3 (6 minutos, cierra dos desviaciones de 5 y 9 días) y la mitad de la #1 que corresponde a MOTOR-10. La #2 es la única reprogramable sin coste acumulativo — pero es el tercer día que se dice eso.

## Riesgos y fechas

**Hitos ≤45 días — dos, ambos Brasil, ambos con la ventana abierta:**

- 🔴 **Brasil · ECA Digital (Lei 15.211/2025), Etapa II — agosto-2026: la ventana es ESTE MES.** La ANPD publica en agosto las *orientaciones definitivas de aferición de edad*. La ronda 2026-08 lo marcó ✅ CONFIRMADO con fuente oficial y lo calificó de **régimen adyacente no cubierto por el repo**. Propuesta editorial pendiente: bloque `minors_digital` en `brasil.json` (aferición de edad, supervisión parental, publicidad a menores). Alta relevancia para devs. Reglamentos de fiscalización/sanciones desde nov-2026; fiscalización desde ene-2027.
- 🔴 **Brasil · Lei 15.352/2026 (25-02-2026): la ANPD pasa a agencia reguladora.** `brasil.json` → `authority` desactualizada, y el archivo está en `verified_editorial`. Es deuda **presente**, no un vencimiento futuro.

**Fuera de ventana (contexto):**

- Chile Ley 21.719 → **01-12-2026, faltan 119 días.** La ventana de instrucciones APDP (jun–nov 2026) sigue **abierta y sin publicaciones**, agravada por el Consejo Directivo aún no constituido. `chile.json` correctamente en `pending_legal_validation`.
- Colombia Ley 2573/2026 → régimen general ~19-11-2026 (**107 días**).
- Consulta pública EDPB sobre Guidelines 02/2026 (anonimización) y 03/2026 (scraping en IA generativa) → cierra **30-10-2026 (87 días)**. Son **borradores**: prohibido citarlas como criterio firme.

**Pendientes conocidos:**

- **Ola 3 (`argentina`, `peru`, `ecuador`, `panama`.json): sin arrancar — 6ª revisión consecutiva.** `cli/rules/latam/` conserva 4 países + `_template.json`. El insumo de Perú está cerrado (ODP exigible desde el 30-06-2026) y el de Ecuador es desbordante (4 resoluciones 2026 + 7 sustantivas de 2025 sin incorporar). **Nota nueva a la luz de MOTOR-10:** añadir países al catálogo **no cambia ningún score** mientras los penalizadores por jurisdicción no tengan call-site. La Ola 3 sigue valiendo (docs, matrices, `legal_refs`), pero su impacto en el motor está condicionado a la decisión de MOTOR-10.
- **Panamá ANTAI-DG-003-2026:** 🟡 PROBABLE por 3ª ronda; verificado el 02-08 que no figura en la sección oficial de Resoluciones ni lo devuelve el Legal Data Hunter. Escalado al editor. Bloquea el `international_transfer` del futuro `panama.json`.
- **AUD-03 (ratificar Ecuador en `strict_regimes`):** desbloqueado desde el 28-07, **7 días sin decidirse**. `EC` en `pending_editorial_decision` con `engine_default: strict`.
- **Fase 7 (web):** `web/src/content` no existe. 6ª revisión sin arrancar, paralelizable, sin bloquear a nadie. Es el trabajo con mejor relación valor/riesgo del backlog no crítico.
- **Fase AP (anti-patrones + `/fix`) e iteración 2 del eval de `/audit`:** sin arranque; `knowledge/anti-patterns/` no existe. Ambas dependen del contrato v1.1.
- **Backlog editorial de la ronda 08:** `colombia.json` → CCM de la Circular 003 · `usa-federal.json` → capa de reglamentos CPPA (ADMT exigible 01-01-2027, evaluaciones de riesgo desde 01-01-2026) · `gdpr.json` → Guidelines blockchain 02/2025, ya finales.
- **Verificación estado-por-estado USA:** cerrada con corrección — **19 leyes integrales, no 20** (IAPP Tracker), y el motor mantiene 19 (aseverado por test).

**Meta-riesgo (sin cambio).** El patrón "días parados + sprint" se mantiene: 5 commits el domingo, 3 el lunes, 0 hoy hasta el momento de esta revisión. Lo que cambió es la **calidad** de lo que se produce en los días activos — dos autocorrecciones documentadas en dos días. Se mantiene la cadencia diaria una semana más, en observación.

---

*Revisión #9 · generada por la tarea programada `revision-diaria-arquitecto-pcs`. Solo lectura: no se modificó código ni contenido legal. No constituye asesoría jurídica.*
