# Revisión del Arquitecto — #5 · 2026-07-29 (miércoles)

**Privacy Compliance Skills (UE · USA · LATAM)** · Revisor: Fable (Arquitecto) · Cadencia: diaria L-V 8:00
*Guía informativa; no constituye asesoría jurídica.*

## Resumen (sin bloqueantes 🟢)

**El día que rompió la parálisis.** Después de 4 días sin movimiento, el 2026-07-28 entraron **22 commits en 2h20** (11:22–13:42): la ronda de vigilancia editorial commiteada, la **Fase 3 completa** (8 tickets) mergeada vía PRs #18 y #19, y una auditoría adversarial de cierre. El **fix T1 — bloqueante desde el 2026-07-09 — está aplicado y con candado de regresión**. Working tree limpio por primera vez en la serie, `develop` respaldado en el remoto, build verde y **77/77 tests** (contra 11 el 23-07). No hay desviaciones técnicas ni degradación de contenido. El cuello de botella se movió: ya no es el arranque, es que **`origin/main` sigue en el estado del 2026-07-09** — el repo público muestra un proyecto 44 commits atrás de la realidad.

## Estado git

- Rama activa: `develop` @ `c4f734d` (2026-07-28 13:42) — **22 commits nuevos** desde la revisión #4.
- **Respaldo remoto ✅:** `develop` ↔ `origin/develop` en **0 / 0**. Todo lo trabajado está pusheado.
- **Working tree limpio ✅** — cero archivos sin commitear (los reportes de revisión 24/27/28-07 entraron en `70971f3`).
- ⚠️ **`origin/main` @ `9f8fbee` (2026-07-09)** — `develop` está **44 commits adelante** de `main`. Además la rama local `main` está **ahead 4 / behind 2** de `origin/main`: desincronizada, hay que reconciliarla antes de abrir el PR de integración.
- Nota conocida: el `.git` montado en Cowork no permite `unlink` del `index.lock` — los commits y el push se hacen en la terminal del usuario o Claude Code. (Confirmado hoy: el warning aparece, pero las lecturas funcionan.)

**Commits de la jornada (agrupados):**

| Bloque | Commits | Contenido |
|---|---|---|
| Editorial | `51c971d`, `70971f3` | VIG-01..07 (adecuación UE en LGPD, Ley 2573/2026 CO, RD 100-2025 PE, gaps state-matrix, insumo Ecuador) + reportes 24–28/07 y backlog |
| Fase 3A (PR #18) | `65fc362`, `587c8ae`, `19bef6d`, `db0300d` | **fix T1** (`strict_regimes` por pertenencia), F_rigor desde `region-factors.json` peor-caso, default seguro Ecuador, regresión BR→UE |
| Fase 3B (PR #19) | `165e6d5`, `1b3123b`, `77ae198`, `c41b209` | Escalado USA multi-estatal, `us_multistate_exposure`, pilar DevOps, `standards_ref` (MOTOR-08 adelantado) |
| Endurecimiento + CI | `a1f3104`, `9ac64d7`, `6fe019b`, `6605627`, `082dd22`, `ee244ad`, `78ac9ac`, `399a1c6`, `cd92c8c` | Fail-loud, `--config` no interactivo, contrato `--json` limpio en stdout, job `cli-tests` en CI |

## Avance vs. plan

> **Desviación de referencia (4ª vez):** el prompt de la tarea programada compara contra `0→H0→A→B→C→D→E→F→G` de `PROMPTS-CLAUDE-CODE-2026-07.md`, documento marcado **histórico el 2026-07-23**. La ejecución vigente es el **roadmap GSD de 8 Fases** (`.planning/ROADMAP.md`). Mantengo ambas vistas — pero con el fix T1 ya cerrado, la vista A–G perdió su último valor informativo. **Recomendación firme: reescribir el texto de la tarea programada contra las Fases 1–8** (acción sugerida, no del top-3 de hoy).

### Vista sesiones A–G (modelo histórico del prompt)

| Sesión | Estado | Evidencia (verificada hoy) |
|---|---|---|
| 0 / H0 (commits Olas 1-2) | ✅ | Obsoletas por banner; trabajo editorial **ya commiteado** (`51c971d`) |
| A (reorg `eu/us/latam`) | ✅ | `cli/rules/eu/gdpr.json` + `latam/*` (5) + `us/*` (2) + `risk-engine/*` (6) + `schema/*` (2) = 16 JSON |
| **B (+H1 fix `strict_regimes`)** | **✅** | `resolveRigorFactor()` y `strict_regimes.members` en `cli/src/engine/rules.ts`; scorer lee `region-factors.json`; **cero comparaciones numéricas de F_rigor**; test de dirección negativa (`399a1c6`) |
| 1.3 (DevOps al engine) | ✅ | `cli/src/engine/devops-scorer.ts` + wizard Q9 + cap `min(sum,30)` del ADR (MOTOR-06) |
| 2.2 (`doctor`) | ⬜ | `cli/src/commands/` sigue con **solo** `audit.ts` — previsto Fase 5 (DOCTOR-01) |
| C (wire USA en skills + dims) | ⬜ | Contenido `us/*` y motor listos; wiring de skills = Fase 6 |
| D (web collections + deploy) | ⬜ | Scaffold `web/` existe; collections = Fase 7 |
| E (validación + golden + fixtures) | 🔄 | `cli/tests/` pasó de **2 → 10 archivos, 77 tests verdes**; CI con job `cli-tests`. Fixtures y golden ⬜ (Fase 5) |
| F (naming + release 0.4.0) | 🔄 | Renombre ✅; `package.json` en `0.4.0-dev`; release ⬜ (Fase 8) |
| G (renombre global código/assets) | ✅ | Sin regresiones de marca |

### Vista GSD (plan vigente)

| Fase | Estado | Nota |
|---|---|---|
| 1 — Higiene, decisiones y renombre | ✅ | |
| 2 — Layout tri-bloque + candado schemas | ✅ | |
| **3 — Motor: F_rigor, fix T1, USA, DevOps** | **✅ COMPLETADA 2026-07-28** | 8 tickets (MOTOR-01..06 + MOTOR-08 adelantado + QA-04); 4 criterios cumplidos con 3 matices documentados |
| 4 — Contrato JSON v1.1 | ⬜ **siguiente** | Alcance restante: 5 tickets CONTRATO-* (MOTOR-08 ya hecho) + MOTOR-09 |
| 5 — Doctor + fixtures + golden | ⬜ | |
| 6 — Skills `/audit` S0–S7 | ⬜ | |
| 7 — Web espejo del repo | ⬜ | Paralelizable desde Fase 2 — **sigue sin arrancar y no bloquea a nadie** |
| 8 — Release v0.4.0 | ⬜ | |

**Progreso: ~20/50 tickets (40%) — +8 en un día** (12/50 el 28-07). Primera vez que la métrica se mueve desde el 2026-07-23.

## Salud del contenido

- **16/16 JSON de `cli/rules/` parsean ✅** (`json.load` sobre `eu/`, `us/`, `latam/`, `risk-engine/`, `schema/`).
- **Build y tests verdes ✅** (verificados hoy sobre copia limpia): `tsc` exit 0 · `node --test tests/*.test.js` → **77 pass / 0 fail** (570 ms).
- **La fórmula sigue viviendo solo en `cli/src/engine/` ✅.** Grep de `F_rigor|score_final|scoreFinal` fuera del engine → **una única coincidencia y es un comentario** (`audit.ts:71`). Cero tablas de factores duplicadas.
- **Archivos protegidos — sin degradación ✅:**
  - `docs/SOURCES-VALIDATION.md`: 259 líneas, diff de la jornada **+34 / −0 (puramente aditivo)**. Secciones **Ola 1** (L34) y **Ola 2** (L127) intactas.
  - `docs/NORMAS-CITADAS.md`: 64 líneas, 5 secciones intactas. Cambio de la jornada: **2 filas enriquecidas** (Brasil → Res. 32/2026 de adecuación UE; Perú → RD 100-2025) con fuente primaria citada. 🟡 **Observación, no desviación:** es edición de contenido en archivo protegido, pero la hizo el **editor humano** (autor del commit: Jose) dentro de la ronda de vigilancia, que es exactamente el flujo que `docs/VIGILANCIA-NORMATIVA.md` §5 le asigna. La restricción de `CLAUDE.md` aplica a los agentes de código, no al editor.
- **`review_status` — promoción conforme ✅.** `brasil.json` y `colombia.json` pasaron de `pending_legal_validation` → `verified_editorial` en `51c971d`. Verificado contra `docs/GOVERNANCE.md:79`: ese estado lo promueve el **equipo editorial interno** y el commit es del owner humano. `reviewed_by` sigue en `null` en los 3 países (correcto: se llena en `validated`, que exige revisor externo). **Nada está `validated`** — la afirmación de `NORMAS-CITADAS.md:62` sigue siendo cierta.
- **Disclaimers intactos ✅** en `DISCLAIMER.md`, ambos README, los 6 `SKILL.md`, los 4 docs y los `metadata.note` de los JSON de país.

## Desviaciones detectadas

Ninguna 🔴 ni 🟠. Riesgos vivos heredados de la auditoría de cierre (todos **documentados y con ticket**, ninguno silencioso):

| # | Riesgo | Dirección | Ticket |
|---|---|---|---|
| 1 | Llave ausente bajo `--config` desactiva su penalizador sin rastro | **Subreporte** (mitigado solo para las 3 llaves estrictas) | CONTRATO-04 (Fase 4) |
| 2 | `ADR-001` documenta `min(100, fe+be)` con cap 50/pilar; el motor usa pool plano | Conservadora (nunca subreporta) | CONTRATO-03 (Fase 4) |
| 3 | `usa-federal.json` sin `legal_refs` ni `fix_hint` → el finding multi-estatal sale sin refs | Vacío **editorial**, no defecto de código | AUD-01 (backlog editorial) |
| 4 | Frontera exacta Chile `2026-12-01T00:00:00Z` sin candado (mutar `>=`→`>` pasa verde) | Cobertura de test | QA-03 (Fase 5) |
| 5 | `countries` sin normalizar dentro del motor (`"br"` → F_rigor 1.00) | Hoy **inalcanzable**; `DOCTOR-01` lo vuelve alcanzable | MOTOR-09 (Fase 4) |

## 3 acciones recomendadas para HOY

1. **🥇 Publicar el trabajo: PR `develop` → `main`.** *(Responsable: usuario, en terminal.)* `origin/main` lleva **44 commits y 20 días** de atraso — el repo público, el README y la web muestran un proyecto sin motor. Ojo al orden: la rama local `main` está **ahead 4 / behind 2** de `origin/main`; reconciliar primero (`git checkout main && git fetch && git reset --hard origin/main` si esos 4 commits locales no aportan nada, verificándolo antes) y luego abrir el PR. Es la acción de mayor retorno del día y no depende de nadie más.

2. **🥈 Arrancar la Fase 4 (contrato JSON v1.1) por `CONTRATO-01`.** *(Responsable: Claude Code — plan con **Opus** (`gsd-planner`), ejecución con **Sonnet** (`gsd-executor`), verificación con **Opus**.)* El motor quedó congelado, verde y auditado: es el momento exacto para congelar la interfaz encima. La Fase 4 es dependencia dura de las Fases 5 y 6 — cada día que no arranca, retrasa el producto central (`/audit` S0–S7). Orden interno ya definido en el roadmap: CONTRATO-01 → MOTOR-08 (hecho) + CONTRATO-02/04/05 → CONTRATO-03 cierra. **Antes de delegar, la semántica del contrato se decide en sesión Fable 5** (así lo fija el propio roadmap).

3. **🥉 Cerrar la decisión editorial de Ecuador (`strict_regimes`).** *(Responsable: usuario como editor técnico-jurídico, apoyado en Cowork.)* El motor está corriendo con un **default seguro que él mismo declara provisional** (`status: pending_editorial_decision`, con `assumption` visible en cada output). La evidencia ya no falta: `knowledge/insumos/ecuador-spdp-2026.md` tiene las 4 resoluciones SPDP 2026 citadas y la recomendación razonada del Arquitecto (5 argumentos a favor, 1 en contra). Es una decisión de 20 minutos que quita una assumption de todos los reportes de Ecuador. **Además, la ronda mensual de vigilancia toca el 2026-08-01 (en 3 días)** — conviene entrar a ella con Ecuador resuelto.

*Secundarias (no del top-3):* AUD-01 (poblar `legal_refs`/`fix_hint` en `usa-federal.json`) · reescribir el prompt de esta tarea programada contra las Fases 1–8 · Fase 7 (web) sigue paralelizable y sin arrancar.

## Riesgos y fechas

- **Nada en la ventana de ≤45 días** (hasta 2026-09-12). El umbral 🔴 de `VIGILANCIA-NORMATIVA.md` §7 no se dispara hoy.
- **2026-08-01 (en 3 días):** ronda mensual de vigilancia normativa (tarea `vigilancia-normativa-mensual-pcs`).
- **2026-11-19 (113 días):** vigencia de la Ley 2573/2026 (CO). Modelada como régimen adyacente con `generates_penalizer: false` y criterio de reevaluación tras esa fecha si hay enforcement.
- **2026-12-01 (125 días):** vigencia de la **Ley 21.719 de Chile**. El motor ya la maneja por `strict_from` con `>=` inclusivo. Las **instrucciones de la APDP siguen saliendo (jun–nov 2026)**: cualquiera puede adelantar el impacto — es el ítem #1 de la ronda del 01-08.
- **Pendientes de largo aliento sin movimiento:** Ola 3 (`argentina/peru/ecuador/panama.json` — V2-06) · Fase AP (anti-patrones + `/fix`) · iteración 2 del eval de `/audit` (Fase 6) · verificación estado-por-estado USA con Descrybe (VIG-04: `state-matrix.json` mantiene el conteo en 19 a propósito, con 4 gaps declarados en `pending_verification`).
- **Riesgo de proceso:** la jornada del 28-07 concentró 22 commits en 2h20. Excelente para desatascar, pero el patrón "4 días parado + 1 día de sprint" hace que la revisión diaria pierda capacidad de corregir a tiempo. Un commit pequeño diario vale más que un sprint semanal.

---
*Reporte generado por la tarea programada `revision-diaria-arquitecto-pcs`. Solo lectura: no se modificó código ni contenido legal. No constituye asesoría jurídica.*
